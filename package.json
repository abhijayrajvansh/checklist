{
  "name": "checklist",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=server && (cd server && pnpm install) || (cd client && pnpm install)",
    "server": "node ./server/server.js",
    "client": "cd client && pnpm run dev"
  },
  "author": "abhijayrajvansh",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
